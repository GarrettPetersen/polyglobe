/**
 * Bump this when land/water rules or inputs change so the demo refetches or you regenerate cache:
 * earth-region-grid.bin, lakes/marine GeoJSON, strait tile IDs, river scalerank/filter,
 * mountains.json, koppen.bin / elevation.bin (PGEL DEM + height model), river rules, or buildTerrainFromEarthRaster options in main.
 */
export const EARTH_GLOBE_CACHE_VERSION = "6-v6";
