# Cloud sprites

`clouds-Sheet.png` is the editable runtime cloud sheet. It contains five 64 by 64
cloud variants in one horizontal row. The game loads these pixels directly at
native resolution.

`clouds.aseprite` is the source artwork. Export the complete 320 by 64 sheet after
editing; keep every cell on the existing 64-pixel grid.
