# Telemetry review, 13 September 2026

The closing scan found the same twelve crash reports in nine contexts as the
opening scan. Two causes required new fixes; the remaining reports have explicit
regression coverage for the existing fixes.

| Failure | Resolution |
| --- | --- |
| Saved hometown tile 23004 resolves to no canonical city | The runtime resolver discarded the name and country supplied by the legacy migration. It now resolves those saved identity fields against the catalog when geography has moved. Authored tile redirections retain precedence; duplicate and ambiguous identities still fail. Tests cover the canonical port catalog, reused tiles, and migration of a frozen v37 save without changing its stored payload. The affected player's exact save was not available, so its hometown is not inferred from the obsolete tile number. |
| Admitted Cadiz root survives without its city scene | Returning through Options removed the scene but retained the dialogue. Menu, duel exit, and restore now release the dialogue and scene together. Failed saves preserve both. The existing browser test left during the greeting, so it missed the admitted-root invariant; it now reaches the city root and evaluates the render-cover check after returning to the menu. |
| Anonymous envoy companions | Existing complete-manifest fix exercised by `telemetryArrivalNavigation.test.js`, including restoration. |
| Wokou target has no sighting position | Existing canonical location and bounded patrol fixes exercised by NPC route, target-rumor, and runtime arrival/navigation tests. |
| Whale ecology clock goes backwards after a duel | Existing clock repair verified through actual Continue with populated, unseeded, and previously corrupted saves. |
| NPC starts outside drawn navigation | Existing collision admission fix verifies placement against current chart geometry and departure from local simulation without destroying the strategic ship. |

The telemetry regression runner now includes the start-menu exit tests and the
two newly addressed crash fingerprints.

## Map diagnostics

One protected chart stitch reported recovery (8.31px error against an 8.10px
constraint). The retained-frame recovery and strict rejection tests pass. No
geography tolerance was widened. This report is classified as successful recovery.

## Performance remains open

Seven low-FPS groups and forty freeze groups remain in the report. Current-build
freezes include rendering, naval weapons, and unattributed frame work. A short
headless combat profile did not reproduce them: CPU p95 3.2ms, maximum frame
10.4ms, no frames over 20ms, and zero map incidents. That result does not establish
that the affected devices or saves are fixed. The performance cursor stays open.

## Validation

All 874 telemetry regression tests passed, along with catalog and source checks,
production module safety, contract types, the production build, and disposable
browser save/menu/duel round trips. No player save or long-running soak was used.
