# Telemetry review, 11 September 2026

Reports read through 21:53 UTC. The cursor must stop at the remembered read time,
so incidents received during this work remain visible for the next review.

## Crashes (eight reports, seven contexts)

| Failure | Resolution and regression coverage |
| --- | --- |
| Campaign homecoming requires a dialogue node | Arrival may be a passenger or quest character rather than a port menu. Only port sessions resolve a menu node. `portQuestSceneReturn.test.js` exercises the real arrival factory and continuation across approach types, greeting state, and loadout state. |
| Returning envoy requires route distances (two reports) | Fixed in `0a894f3d`; `questJourneyDialogue.test.js` verifies returning/recalled envoys do not require an outbound briefing. Invalid outbound routes still fail. |
| Unknown assault sound `deck-land` | Fixed in `5f300304`; `soundEffectContracts.test.js` runs actual assault events through the sound dispatcher and validates all declared event policies, sound pools, assets, and credits. |
| Worker trade quantity zero (two build contexts) | Fixed in `099435d7`; `npcSeaRoutes.test.js` covers a commissioned merchant arriving after storage fills, fractional remaining storage, and subsequent resumption. |
| Caravel render asset network failure | Explicit retry flow added in `90732e7d`; `worldAssetRetry.test.js` preserves paused gameplay while offline and resumes only after successful retry. Invalid assets remain errors. Reported asset returned HTTP 200 on review. |
| Itch faction manifest 404 | Packaging and startup verification fixed in `e8aed9b9`; `startupAssetVerifier.test.js` checks missing startup assets. This does not establish that an old downloaded itch archive has been replaced. |

The telemetry regression runner now includes the asset, journey, assault sound,
and benchmark measurement contracts. The full runner also exposed an outdated
Exeter fixture; it now exercises minimap redraw and invalidation during canal
construction, plus restoration before a minimap exists.

## Map diagnostics (five contexts)

The 9.1px sailing correction is covered by `sailingContinuity.test.js`: legitimate
wave-driven settlement is separated from a newly introduced discontinuity. The
other four contexts explicitly report recovered protected-stitch conflicts.
`localLayoutAdmission.test.js` verifies retained-frame admission preserves visible
anchors, while strict admission still rejects incompatible constraints.

## Performance (47 contexts)

Seven low-frame-rate groups and forty freeze groups remain under investigation.
They include terrain rendering, city rasterization, politics, NPC scheduling,
saving, and scheduler delays. Older build numbers alone are insufficient evidence
of a fix. The performance cursor must remain open until these are resolved or
classified with evidence.

The paused-overlay performance oracle now rejects absent, non-finite, negative,
or wrongly typed measurements instead of accidentally passing comparisons against
`undefined` or `NaN`. Regression tests also reject corrupt stage counts.

## Verification

The expanded telemetry regression runner passed all 777 tests after repairing the
fixtures. Targeted retained-frame, sailing-continuity, and commissioned-supply
boundary tests passed. Catalog verification, production safety, contract types,
and the full production build passed. No long-running soak or player save was used.

A short headed busy-world probe passed on native Apple Silicon Chrome: 54.15
rendered FPS, CPU p95 5.1ms, maximum frame 33.7ms, and zero map incidents. An
Intel-Node-launched Chrome probe ran under translation and produced unusably slow
measurements; use native Node/Chrome for performance comparisons on Apple Silicon.
This local probe is not evidence that all historical low-end-device freezes are fixed.
